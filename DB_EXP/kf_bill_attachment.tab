create table KF_BILL_ATTACHMENT
(
  id          VARCHAR2(32),
  bill_code   VARCHAR2(32),
  file_name   VARCHAR2(255),
  file_size   INTEGER,
  file_url    VARCHAR2(255),
  file_type   VARCHAR2(32),
  upload_per  VARCHAR2(60),
  upload_date DATE,
  hire_code   VARCHAR2(32)
)
;

