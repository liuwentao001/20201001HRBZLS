CREATE OR REPLACE FUNCTION HRBZLS.FGETOPERFUNCDATE(VOAID IN VARCHAR2,
                                            VFID  IN VARCHAR2) RETURN DATE AS
  LDATE DATE;
BEGIN
  SELECT ORFEXPIRYDATE
    INTO LDATE
    FROM OPERACCNTROLEFUNC_EX
   WHERE ORFOAID = VOAID
     AND ORFFID = VFID;
  RETURN LDATE;
EXCEPTION
  WHEN OTHERS THEN
    RETURN NULL;
END;
/

