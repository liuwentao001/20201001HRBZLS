CREATE OR REPLACE FUNCTION HRBZLS.F_水表状态清单(P_NUM IN NUMBER) RETURN VARCHAR2 as

begin
/* CASE 
   WHEN 1 THEN RETURN '立户';
   WHEN 2 THEN RETURN '预立户';   
   WHEN 3 THEN RETURN '';   
   WHEN 4 THEN RETURN '';   
   WHEN 5 THEN RETURN ''; */
     
   return '';

end F_水表状态清单;
/

